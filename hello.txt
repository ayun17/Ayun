아무말 아무말
Ctrl + S
ctrl + ~
배시셸에서 글이 안써질때 ctrl +  c를 누르고 enter 
git init 은 이 디렉토리를 깃에서 관리하는 디렉토리로 만드는 것
스테이지에 더하는 명령어는
git add 파일명 (위 화살표 2번 누르기)
git commit -m "내 첫번째 커밋"

git config --global user.name "이름"
git config --global user.email "이메일"

add